/**
 *
 * {{ properCase name }}
 *
 */

import * as React from 'react';

{{#if wantMessages}}
import { FormattedMessage } from 'react-intl';
import messages from './messages';
{{/if}}
{{#if wantCSS}}
const styles = require('./styles.css');
{{/if}}

class {{ properCase name }} extends React.Component<any, any> { // eslint-disable-line react/prefer-stateless-function
  public render() {
    return (
      {{#if wantCSS}}
      <div className={{curly true}}styles.{{ camelCase name }}{{curly}} {...this.props}>
        <div>hello world</div>
      {{else}}
      <div>
      {{/if}}
      {{#if wantMessages}}
        <FormattedMessage {...messages.header} />
      {{/if}}
      </div>
    );
  }
}

export default {{ properCase name }};
